/* eslint-disable */
// @ts-nocheck
// NEUTRALIZED: this page depends on closed-source Papermark EE (branding/presets),
// which is stubbed in this self-hosted build. Rendered inert so the build can prerender.
export default function Page() {
  return null;
}
